# Copy this file to .env.local and fill in your Worker URL.
# Only needed if you want to use the Cloudflare Worker locally
# instead of the local Fastify backend (npm run dev).
VITE_WORKER_URL=https://your-worker.your-username.workers.dev
